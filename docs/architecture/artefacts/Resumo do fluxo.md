Fontes (Wikipedia + Google Trends)
    ↓
Bronze (dados brutos)
    ↓
Silver (limpos, validados, integrados)
    ↓
Gold (analíticos: origem + propagação)
    ↓
API (endpoints)
    ↓
Frontend (mapa + slider + dropdown)